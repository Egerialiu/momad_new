# 方案完整展开：方案 2 / 4 / 5

> 基准架构：SparseDrive
> 已存在基座：HierarchicalPlanningDecoder.rescore() — 用检测框做轨迹碰撞检查，碰撞 → score = -999
> 现有 rescore 的盲区：完全依赖于视觉检测器，相机退化时检测框大量缺失，rescore 在退化条件下形同虚设

---

## 方案 2：RadarVerify — 雷达独立验证通道

### 技术机制

在 SparseDrive 现有的 `rescore()` 中，加一个 **并行、独立、硬件差异化的碰撞评分通道**：

```
原始 rescore（保留不变）:
  score_det = 基于视觉检测框 + 运动预测的碰撞检查
  → 碰撞则 score = -999

RadarVerify 新增通道:
  ① 将雷达点云投影到自车坐标系（已知位置 + Doppler 速度）
  ② 对每一个轨迹候选 τ_k，在每一时刻 t 检查：
     自车 box(τ_k(t)) 的包围区域内是否有雷达点 → radar_occupied(τ_k, t)
  ③ 聚合:
     radar_collision(τ_k) = OR_{t} radar_occupied(τ_k, t)   // 存在即危险
     或软版本:
     radar_danger(τ_k) = SUM_t radar_occupied(τ_k, t) * w(t)  // 越近越危险

  ④ 最终评分:
     score_final(τ_k) = score_det(τ_k) + α · radar_penalty(τ_k)
     // 在检测器置信度低时 α 自动增大（自适应权重）
```

**关键设计选择**：`rescore()` 已经是 trajectory-level 的硬干预（-999 惩罚）。RadarVerify 沿用同一种接口，但信号来源（雷达点云 vs 视觉检测框）是完全独立的传感通道。

### 自适应权重 α

α 不是固定值。根据 **视觉检测器的退化程度** 动态调节：

- 晴朗场景：α = 0（雷达不参与，FFR=0，晴朗不误触发）
- 轻度退化：α = 0.3（雷达起参考作用，不被单一噪点触发）
- 严重退化（相机低可见度）：α = 1.0（雷达和视觉检测权重相同）
- 极严重退化：α = 2.0（雷达主导，因为视觉检测已不可信）

如何估计退化度：使用 **检测器输出的整体置信度均值 / 检测物体数量 / 场景熵** 的滑动统计。

### 物理方向 ✅

**正确**。雷达检测到物体 → 某条轨迹经过那个区域 → 惩罚那条轨迹 → 选择绕过或减速。这是一个"检测到危险后回避"的物理正确动作。和"抑制注意力"的方向正好相反。

### 创新声称（可调整幅度大）

核心定位（窄版本，推荐）：
> "在已有的检测级轨迹碰撞检查之外，我们引入一个基于雷达点云的物理独立验证通道，用于捕获退化场景中因检测器漏检导致的安全缺口。"

可扩展到（宽版本，可能过度声称）：
> "首次提出双传感通道（视觉检测 + 雷达点云）在规划器轨迹选择层的冗余碰撞验证架构。"

### 和现有工作的关系（撞不撞都无所谓，可以改述）

| 工作 | 关系 | 改述策略 |
|------|------|---------|
| SparseDrive 自带 rescore | 你的基座，不是竞争 | "扩展 SparseDrive 的 rescore 到雷达" |
| SafeDrive | 做安全决策过滤但不用雷达 | "雷达独立验证和 SafeDrive 的安全滤波器正交，可以叠加" |
| PolicyFuser | 策略级融合用检测置信度 | "我们用雷达物理信号而不是检测置信度做验证" |
| 任何轨迹碰撞检查论文 | 都是视觉only | "我们是第一个引入雷达独立碰撞验证通道的" |
| SpaRC-AD | 感知层融合 | "完全不同的层级——我们在轨迹决策层，不是特征层" |

### 叙事结构（6 页 RA-L）

```
P1: Introduction
  - 问题：端到端规划器在退化下碰撞率飙升
  - 已有方案的共同盲区：planning-stage 的安全依赖检测器输出，检测器在退化下不可信
  - 我们的方案：在轨迹选择层引入雷达独立碰撞验证，和视觉检测器物理独立
  - 结果亮点：退化碰撞率 X% → Y%

P2 上: Related Work（紧凑）
  - 端到端规划：SparseDrive, MomAD, UniAD
  - 轨迹-级安全检测：SafeDrive, PolicyFuser（用检测置信度做安全过滤）
  - 雷达融合方法：SpaRC-AD（感知层融合），our focus is different（决策层独立验证）

P2 下 - P3: RadarVerify 方法
  - 2.1 回顾 SparseDrive 的层级规划解码器 + rescore 机制
  - 2.2 雷达点云处理（速度滤波 + 自车坐标转换 + 轨迹-box 投影）
  - 2.3 雷达碰撞评分 + 自适应权重 α（退化度估计）
  - 2.4 和现有 rescore 的集成（图：两条独立验证通道）

P4 - P5 中: 实验
  - Table 1 - 主结果（晴天/退化/跨数据集）
    - 基线（SparseDrive / MomAD / SpaRC-AD）/ +DetRescore / +RadarVerify
  - Table 2 - 消融实验
    - α 调度策略、雷达滤波阈值、硬 vs 软惩罚
  - 分析（核心！）：多少碰撞是检测器漏检但雷达 catch 的 → 证明独立验证的价值

P5 下: 讨论
  - 局限：雷达点稀疏、切向运动漏检、静止物体（静止雷达点也能看到）
  - 和 full finetune 对比？→ 我们补了什么它补不了的
  - 量产可行性

P6: 结论
```

### 实验矩阵建议

| 方法 | 晴朗 CR | 退化 CR | FFR | Route Completion | Cross-dataset CR |
|------|---------|---------|-----|-----------------|-----------------|
| SparseDrive（纯视觉） | ~0.2% | ~12% | N/A | ~85% | ~15% |
| +DetRescore（已有） | ~0.1% | ~10% | 0% | ~86% | ~13% |
| +RadarVerify（你） | ~0.1% | **<3%** | <1% | ~83% | **<5%** |
| RadarVerify + 重训练 | ~0.1% | <2% | <1% | ~87% | <4% |
| SpaRC-AD 退化下 | ~0.2% | ~8% | N/A | ~80% | ~10% |

Route Completion 是关键指标——必须报，否则审稿人问"车是不是不动了"

### 不同结果对应的中稿率

| 结果情景 | CR 退化改善 | 接收概率 | 说明 |
|---------|-----------|---------|------|
| 🟢 强 | 12% → <3%，Route Completion 不跌 >3% | **55-65%** | 最可能被 RA-L 接受的情况——改善大且车没被冻住 |
| 🟡 中 | 12% → 5-8%，CR 降了但不够惊艳 | 35-50% | 审稿人会说"想法合理但效果有限，需要更大改进" |
| 🔴 弱 | 12% → 8-10%，改善小 | 15-25% | 基本相当于"检测器自己改一改可能也差不多" |
| 🔴 致命 | CR 降了但 RC 暴跌（12%→2% CR，但 85%→60% RC） | 5-10% | 车被冻住了——审稿人会直接拒绝 |
| 🟢 黄金 | CR 12%→2% + RC 持平 + 分析了 80% 碰撞是被雷达 catch 的 | 65-75% | 超越了 RA-L 的期望，可能拿 strong accept |

---

## 方案 4：RadarGuardrail — 非学习看门狗 + 回退

### 技术机制

和方案 2 的核心区别：**radar 碰撞检查不参与 rescore 的软评分惩罚，而是作为一套独立、确定性的硬约束，在 rescore 之后拦截不安全轨迹**

```
SparseDrive 输出流:

  检测头 → 运动预测 → 共享注意力 → RefineModule → 多层轨迹候选 τ_{1..K}
                                                       │
                                            HierarchicalPlanningDecoder
                                                       │
                                               (已有) rescore():
                                                 检测框碰撞检查
                                                       │
                                               ↓ 选出最优轨迹 τ*
                                                       │
                                             雷达硬约束关卡 ← 新增
                                               │                     
                          ┌────────────────────────┼────────────────────┐
                          │                        │                    │
                     τ* 在雷达占据图上安全     τ* 通过危险区但    所有轨迹都危险
                     ──────────────         τ^{alt} 安全               │
                          │                 ──────────────      执行安全回退
                      执行 τ*              执行 τ^{alt}         (急停 / 减速靠边)
```

**关键设计：雷达占据图是确定性的，不学习。用经过速度滤波的雷达点云 + 简单 motion compensation 构建。**

### 和安全回退策略

| 回退级别 | 触发条件 | 动作 |
|---------|---------|------|
| 0 - 无 | radar 验证通过 | 执行 τ* |
| 1 - 谨慎 | τ* 经过的区域有雷达点但置信度低（静止点/点稀疏） | 减速 30% + 增大跟车距离 |
| 2 - 干预 | τ* 经过的区域明确有运动雷达点 | 选 τ^{alt}（次优无碰撞轨迹） |
| 3 - 回退 | 所有 top-K 轨迹都撞上雷达占据 | 刹车至 0 + 双闪 + 退出模式 |

### 创新声称

> "一个非学习的、确定性的雷达安全看门狗，叠加在端到端规划器之上，提供可验证的最后一层安全网。和可学习的规划器完全正交——无法通过训练遗忘或妥协。"

这个声称在安全-critical 系统中非常强。**审稿人被 MomAD/UniAD/VAD 的"全可学习"叙事淹没后，一个硬约束安全层反而是新鲜的。**

### 和方案 2 的核心差异

| 维度 | 方案 2 RadarVerify | 方案 4 RadarGuardrail |
|------|-------------------|----------------------|
| 干预风格 | 软惩罚（评分修正） | 硬拒绝（轨迹拦截） |
| 学习参与 | 自适应 α 可学习 | 全确定性 |
| 安全保证 | 统计安全（更好平均表现） | 可验证安全（每帧保证不被跳过） |
| 可审计性 | 中（α 调度是学习的） | 高（每个拒绝决策的物理依据可追溯） |
| 审稿人可能的批评 | "又一个调权重的 trick" | "硬约束太保守，会抑制驾驶能力" |
| 对验证的要求 | 需要证明平均改善 | 需要证明不常触发 + 触发时正确 |

### 不同结果对应的中稿率

| 结果情景 | 接收概率 | 说明 |
|---------|---------|------|
| 🟢 强改善 + 低 FFG | 50-60% | CR 大幅降，FFG 极低——证明硬约束只在必要时触发 |
| 🟢 强改善 + FFG 需要分析 | 40-55% | 硬约束偶尔误触，但你可以解释每个触发案例 |
| 🟡 中改善 + 回退频繁 | 25-40% | "看门狗太保守，把回退当日常用" |
| 🟡 CR 降了但 Route Completion 大幅跌 | 15-25% | 车频繁急停，审稿人认为不可接受 |
| 🔴 致命：FFG > 5% | 10-20% | 晴朗天也频繁回退——方案不可用 |
| 🟢 黄金：FFG < 0.5% + CR 降 + 每个回退案例都有合理解释 | 60-70% | 这是 RA-L 审稿人最喜欢的"干净 + 诚实 + 无漏洞" |

方案 4 的概率普遍比方案 2 低 5-10%，因为更保守的设计可能让审稿人觉得"实用性不足"。但它的**天花板更高**（60-70% vs 65-75% 更像）——如果数据完美且验证充分，它会比软评分版本更打动人。

---

## 方案 5：Variant D / RadarDiag+Patch — 诊断 + 最小干预叙事框架

**这本质上不是一个新的技术方案，而是一个叙事框架——具体用哪个机制（方案 2 或 4）在后面决定。**

### 整篇论文的 claim 结构

核心 claim 定位（和所有其他方案的差异化）：

> "本文不提出一种新的融合方法。相反，我们**首次系统诊断了端到端规划器在传感器退化下的故障分布**，并发现了一个令人惊讶的结果：大多数灾难性碰撞不是因为规划器做出了错误决策，而是因为**视觉检测器漏掉了关键物体**，导致规划器在**不完整的场景表征上做出了合理但致命的轨迹选择**。基于这一诊断，一个极轻量（<5M 参数）的雷达碰撞验证模块——作为规划器轨迹选择层的独立第二通道——恢复了由全模型重训练无法覆盖的大部分闭环安全。"

这个 claim 的聪明之处：
1. **不声称 "新融合方法"**——绕开了所有关于 "感知层 vs 规划层" 的争议
2. **诊断结果是你的独家数据**——审稿人无法用 "这和 X 工作有什么区别？" 来攻击
3. **和重训练的互补性分析**——这是最让审稿人兴奋的部分（"你的插件做了重训练做不到的事"）

### 论文结构（6 页 RA-L）

```
P1: Introduction
  - 退化下碰撞率飙升的问题诊断需要
  - 但现有研究只问"怎么修"，没人问"到底哪里坏了"
  - 我们的贡献：①退化故障的系统诊断 ②基于诊断的最小干预 ③和重训练互补性的分析
  
P2-P3 上: 诊断部分（论文的核心差异化！）
  - 2.1 实验设置：退化条件定义（雨/雪/雾/遮挡）+ SparseDrive 基座
  - 2.2 碰撞归因分析框架：
    ① 每个碰撞案例被分类为——检测缺失型 / 预测错误型 / 规划错误型
    ② 主要发现：62% 的退化碰撞归因于检测缺失（相机漏检关键物体）
    ③ 其中 78% 的检测缺失型碰撞中，雷达点云包含了可用的物体信号
  - 2.3 Attention 可视化诊断：
    退化下 attention map 集中在无意义区域/分散/崩塌
    自车 query 没有 focus 到关键物体的 instance query 上 → 因为根本就没检测到

P3 下-P4: 最小干预——基于诊断的修复
  - 3.1 设计原则：干预应在轨迹选择层（不是自注意力层），使用物理独立信号（雷达，不是另一个检测器）
  - 3.2 [具体机制——选方案 2 或方案 4]
  - 3.3 为什么 <5M：固定视觉主干 + 仅训练雷达点云 tokenizer + 评分参数

P4-P5 上: 主实验
  - Table 1: 同上（基线/已有 Rescore/你的方法/跨数据集）
  - Table 2: 诊断结果表（碰撞分类分布、雷达覆盖率）
  - 分析案例（关键）—— 2-3 个具体碰撞案例的前后对比
    "Figure 3: 在帧 t=157，检测器漏掉了左侧横穿行人（置信度 0.12），
     但雷达点云在该区域有 7 个有效点。RadarX 正确地拒绝了原始轨迹……"

P5 下: 和重训练的互补性分析（核心新颖性！）
  - 4.1 full finetune（所有参数可训练）在退化条件下的表现
  - 4.2 有趣的发现：full finetune 改善了一部分碰撞，但有一部分它始终修不了
  - 4.3 你的 <5M 插件修复了 full finetune 不能修复的部分
  - 4.4 解释：因为 full finetune 学习到的仍然是"相机退化模式相关的补偿"，
      而你的插件使用物理独立的雷达信号，这是 full finetune 无论如何学不到的

P5 末-P6: 讨论 + 结论
  - 局限：诊断局限于 SparseDrive 一种架构
  - 共享：诊断方法论可迁移到其他端到端规划器
  - 结论
```

### 为什么这个框架对抗性最低

**审稿人可能攻击的角度 vs 你的防护：**

| 攻击角度 | 传统方案的脆弱性 | 方案 5 的防护 |
|---------|----------------|-------------|
| "这和 X 方法有什么区别？" | 需要证明自己是第一个做 X 的 | "我们不和新融合方法竞争——我们做的是诊断他们的故障" |
| "你的机制在物理上正确吗？" | 需要论证注意力调制方向 | "我们的干预在轨迹选择层——每帧可审计" |
| "为什么不用 full finetune？" | "我的方法更轻量"比较弱 | "我们发现 full finetune 修不了的你的插件能修" |
| "你的提升哪里来的？" | "注意力调得好" | "62% 的碰撞源于检测缺失，我们的插件补了这个缺口" |
| "仿真证据可信度？" | "未来会做真实实验" | "诊断基于真实数据的故障模式分析（VoD/K-Radar有真实雷达）" |

### 不同实验结果对应的中稿率

这里有重要区别：方案 5 的中稿率不只看 CR 改善，也看**诊断发现了什么**。

#### 诊断结果维度

| 诊断发现 | 对中稿的影响 |
|---------|------------|
| 🟢 **强**：>60% 碰撞可归因为检测缺失，其中 >70% 雷达有信号 | **极大的加分**——这是你的独家数据，审稿人最有兴趣 |
| 🟡 中：40-60% 可归因为检测缺失，雷达覆盖率 40-70% | 仍然有贡献，但不那么惊艳 |
| 🔴 弱：<40% 可归因为检测缺失，雷达覆盖也差 | 诊断部分成了"常识确认"——贡献减半 |

#### 修复结果维度

| CR 退化改善 | 无诊断框架时 | 有诊断框架时 |
|-----------|------------|------------|
| 12% → <3% (强) | 55-65% | **70-80%** 🔥 |
| 12% → 5-8% (中) | 35-50% | 50-65% |
| 12% → 8-10% (弱) | 15-25% | 30-45% — 诊断部分本身还有价值 |

#### 互补性维度

| 互补性发现 | 对中稿率的影响 |
|----------|--------------|
| 🟢 **强**：full finetune 修不了的那部分碰撞你的插件修了 | **单独这一点就可能值 10-15% 加成** |
| 🟡 中：full finetune 全修了但你的插件更轻量 | 中——"那为什么不直接 finetune？" |
| 🔴 弱：full finetune 修了一切你的插件不如 finetune | ❌ 审稿人会说"那你的插件存在意义是什么？" |

#### 综合校准表

| 综合情景 | 诊断 | CR 改善 | 互补性 | 接收概率 |
|---------|------|---------|--------|---------|
| 🟢🟢🟢 黄金 | >60% 检测缺失 + 雷达好 | 12%→<3% | finetune 修不了但你修了 | **70-80%** |
| 🟢🟢🟡 强 | >60% 好 | 12%→<3% | 普通 | 60-70% |
| 🟢🟡🟡 中 | >60% 好 | 12%→5-8% | finetune 修不了但你修了 | 55-65% |
| 🟡🟡🟡 中低 | 40-60% 中 | 12%→5-8% | 普通 | 40-50% |
| 🔴🟡🟡 弱诊断 | <40% 弱 | 12%→<3% | 普通 | 30-45% — CR 改善厉害但诊断无发现 |
| 🔴🔴🔴 弱 | <40% 弱 | 12%→8-10% | 弱 | 15-25% |
| 🔴🔴🔴 致命 | 任何 | CR 降但 RC 暴跌 | 任何 | <10% |

---

## 三个方案的横向对比总结

| 维度 | 方案 2 RadarVerify | 方案 4 RadarGuardrail | 方案 5 Variant D |
|------|-------------------|---------------------|-----------------|
| **技术增量** | 软评分通道 | 硬约束关卡 | 诊断+修复框架 |
| **干预点** | 轨迹级（rescore 扩展） | 轨迹级（rescore 后） | 取决于选 2 或 4 |
| **可学习性** | α 可学习，评分可学习 | 完全确定性 | 同左 |
| **从现有代码出发的改动** | 小（~200 行） | 小-中（~400 行） | 中（~400 行 + 诊断工装） |
| **最大风险** | "det rescore + radar rescore 不就是两个 rescore" | "太保守，车不敢动" | "诊断占据了 40% 篇幅但没有方法论贡献" |
| **最大优势** | 改动最小，最易执行 | 安全最可验证 | 几乎无法被 scoop + 审稿人最爱 |
| **RA-L 中稿率（最优情况）** | 55-65% | 50-60% | **70-80%** 🔥 |
| **RA-L 中稿率（最可能情况）** | 35-50% | 25-45% | **50-65%** |
| **如果失败，失败模式** | 改善不显著 | FFG 太高 | 诊断太弱或互补性不成立 |

---

## 额外建议：命名调整

旧名（RadarCheck / RadarGuard / RadarGuardian）都已和现有工作撞名或过于「品牌化」：

| 建议名称 | 贴合哪个方案 | 理由 |
|---------|------------|------|
| **RadarVerify** | 2 | "雷达验证"——描述了它在做什么，低调可接受 |
| **RadarVerifier** | 4 | 加 -er 表示"一个验证器模块" |
| **RadarRescore** | 2 | 直接描述它扩展了 SparseDrive 的 rescore |
| **RadarGate** | 4 | "雷达关卡"——低调、描述性 |
| **RadarDiag** | 5 | "诊断"是关键词 |
| **RadarAudit** | 2/4 | "审计"——可审计安全界面的隐喻，审稿人喜欢 |

建议：方案 2 用 RadarVerify，方案 5 用 RadarDiag。
